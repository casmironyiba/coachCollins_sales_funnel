import { FC } from "react";
import styled from "styled-components";

const DIV = styled.div`
  width: 100%;
  height: 150px;
  background: #333333;
`;

const Footer: FC = () => <DIV>@copy;</DIV>;
export default Footer;
